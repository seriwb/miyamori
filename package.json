{
  "name": "miyamori",
  "version": "1.0.0",
  "description": "Bot to input attendance from Slack.",
  "main": "dist/app.js",
  "scripts": {
    "ngrok": "ngrok http 3000",
    "dev": "babel src -d dist && node --require dotenv/config dist/app.js",
    "start:dev": "node --require dotenv/config dist/app.js",
    "build": "babel src -d dist",
    "start": "node dist/app.js",
    "clean": "rm -rf dist",
    "lint": "eslint src/**",
    "test": "mocha --compilers js:babel-core/register src/test/**/*.js"
  },
  "keywords": [],
  "author": "seri",
  "license": "MIT",
  "dependencies": {
    "@slack/bolt": "^3.10.0",
    "config": "^3.3.7",
    "jfs": "^0.3.0",
    "js-yaml": "^4.1.0",
    "log4js": "^6.4.2",
    "sqlite3": "^5.0.2"
  },
  "devDependencies": {
    "@babel/cli": "^7.17.6",
    "@babel/core": "^7.17.5",
    "@babel/preset-env": "^7.16.11",
    "dotenv": "^16.0.0",
    "eslint": "^8.11.0",
    "eslint-plugin-import": "^2.25.4",
    "ngrok": "^4.3.1"
  }
}
